# Changelog

## [0.2.7]
- Improved `demo_fetch_example` with inline notes to prevent misuse of `select`.
- Added lightweight sanity check to preview first few Works from standard JSONL.
- No API changes. No output format changes.

## [0.2.6]
- Refined README (EN/JA) to align documentation with current fetch behavior.
- Documented optional query controls (type/sort/select) and safe defaults (select none).
- No API changes. No output format changes.

## [0.2.5]
- Documentation update only (README positioning and scope clarity).
- Clarified acquisition-only scope and stop/move-on guidance in README.
- Adjusted workflow positioning wording and removed redundant sections.
- No API changes. No output format changes. No code changes.

## [0.2.4]
- Documentation update only (README positioning and scope clarity).
- No API changes. No output format changes. No code changes.

## [0.2.3]
- Documentation update only (README positioning and scope clarity).
- No API changes. No output format changes. No code changes.

## [0.2.2]
- Documentation update only (README positioning and scope clarity).
- No API changes. No output format changes. No code changes.

## [0.2.1]
- Documentation-only release.
- Simplified README opening and made repository role explicit (acquisition-only).
- Added clear "stop here / move on" criteria early in README.
- Linked explicitly to downstream repositories (normalize / analyze).
- No functional changes. Data formats and runtime behavior unchanged.

## [0.2.0]
- Added standard JSONL export (1 Work per line).
- Added CSV export (lossless; nested fields preserved as JSON strings).
- Added optional polite pool (`mailto`) support in example script.
- Clarified repository scope and non-goals in README.

## [0.1.1]
- Initial public release with cursor-based fetching and resumable checkpoints.