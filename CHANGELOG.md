# Changelog

## [0.2.0]
- Added standard JSONL export (1 Work per line).
- Added CSV export (lossless; nested fields preserved as JSON strings).
- Added optional polite pool (`mailto`) support in example script.
- Clarified repository scope and non-goals in README.

## [0.1.1]
- Initial public release with cursor-based fetching and resumable checkpoints.